apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: my-daemon
  labels:
    app: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        name: example-pod
        env: devops
    spec:
      containers:
      - name: example-container
        image: nginx
        ports:
        - name: http
          containerPort: 80